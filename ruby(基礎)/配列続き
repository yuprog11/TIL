メソッドが返した値のことを？                                                  戻り値
組み込みライブラリとは？                                                     rubyですぐ使える道具。配列とか
クラスとは？                                                              オブジェクトの種類
ブロックとは？                                                             do　〜ｅｎｄのような処理をかける部品、かたまり
引数とは？                                                               メソッド（putsとか）へ渡すオブジェクト（中身）のこと
{}()||
"#{x}円"書き方使い方
リファレンスマニュアル全体ざっと読む、書く時に予習になる
メソッドは同じ機能で名前が違うものがある　文脈、好みで使い分ける

破壊的変更とは？                                                          普通メソッド使って処理結果変わるけど、a,bとがあって、bで破壊的メソッド使うとaを含めた処理の場合、aも処理結果後を表示する
破壊的メソッドを使ったかどうか確認するためには                                    

                                                                       array1 = [1,1,2]
                                                                       array2 = array1.uniq
                                                                       
                                                                       p array1.object_id  #=>123456789
                                                                       p array2.object_id  #=>457894561     番号が違うから破壊的メソッド使ってない　とうらい通りオブジェクト二つある
                                                                       
                                                                       
                                                                       
                                                                       array1 = [1,1,2]
                                                                       array2 = array1.uniq!
                                                                       p array1.object_id  #=>123456789
                                                                       p array2.object_id  #=>123456789     番号が同じ（＝オブジェクト1個　この1個にarray1、array2のなふだをつけてる）だから破壊的メソッド　　になる
                                                                       オブジェクト・・中身、もの　元の配列を残したいかどうかで使い分け

ブロック →　{ |n| n.to_s }
ブロックが与えられるorブロックを渡す　→ uniq{ |n| n.to_s }　このようにメソッドの後ろにブロックを書いていること
sortメソッド、reverseメソッド　2つの効果組み合わせたい場合                           p [2,5,8].sort.reverse のように書く
joinメソッド                                                              配列の中身をつなげる　引数を渡すと（”と”　とか）要素の間に引数が入る
splitメソッド                                                             中身を区切る　引数を渡すと（”と”　とか）該当する因数のところで区切る
mapメソッド                                                               新しい配列を作るメソッド。繰り返し処理みたいな書き方で　変数*2（変換処理）とかしたらその結果を配列にする 最初に定義した変数をpとかで表示
sampleメソッド                                                            配列の中身をランダムに表示　おみくじプログラムとか作れる感じ
sortメソッド                                                              数字は小さい順　文字列はabc順　.reverseで逆に 
reverseメソッド                                                           文字列に対すると回文のようになる
split メソッド                                                            文字列を配列にする　p ”あ　い　え　え”.split とかだと半角の箇所で区切って配列にして引数渡せばその引数に該当する箇所で区切って配列にする 
